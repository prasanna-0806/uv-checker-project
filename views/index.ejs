<!DOCTYPE html>
<html>
<head>
  <title>UV Results</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Manrope:wght@500;700&display=swap" rel="stylesheet">

</head>
<body>
  <div class="main">
  <h1>🌞 UV Results</h1>

  <h2>Current UV: <%= uvData.result.uv %></h2>
  <p>Max UV Today: <%= uvData.result.uv_max %></p>
  <p>Reading Time: <%= uvData.result.uv_time %></p>

  <% if (uvData.result.uv < 3) { %>
    <p class="safe">✅ Low risk. No sunscreen needed.</p>
  <% } else if (uvData.result.uv < 7) { %>
    <p class="moderate">⚠️ Moderate. Wear SPF 30+ sunscreen.</p>
  <% } else { %>
    <p class="high">🚫 High UV! Avoid direct sun.</p>
  <% } %>

  <br>
  <a href="/" class="back-button">← Back to Home</a>
    </div>
</body>
</html>
